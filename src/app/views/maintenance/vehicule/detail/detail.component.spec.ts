import { DetailComponent } from './detail.component';

describe('DetailComponent', () => {
  let component: DetailComponent = new DetailComponent();

  beforeEach(() => {
    component = new DetailComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
